(() => {
  //TypeScript
  function suma(a: number, b: number): number {
    return a + b;
  }

  function holaMundo(): string {
    return "Hello, World!";
  }
  //TypeScript sin retorno
  function imprimirNombre(yourName: string): void {
    console.log(`Hello ${yourName}`);
  }
})()
